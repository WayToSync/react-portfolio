import React from 'react';
import styled from 'styled-components';

const WorksPresentation = () => {
  return <WorksWrapper></WorksWrapper>;
};

const WorksWrapper = styled.div`
  height: 805px;
  width: 1200px;
  background-color: black;
`;

export default WorksPresentation;
